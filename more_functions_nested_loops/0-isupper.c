#include "main.h"

/**
 * _isupper - check for uppercase
 * @c: char
 * Return: 1 if c uppercase else return 0
 */

int _isupper(int c)
{
if (c <= 64  && c <= 90)
{
return (1);
}
else
{
return (0);
}
}

